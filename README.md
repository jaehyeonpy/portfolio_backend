# Notes on codes:
- a try-except statement is implemented, unless an letting error-causing statement occur an error is effective.
- All the codes use third-party redis python client libraries, because django does not provide advanced features as those libraries do.
